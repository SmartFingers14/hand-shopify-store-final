<aside class="mob-menu" id="mobMenu" aria-hidden="true">
  <div class="mh">
    <div class="logo" style="color:var(--bg);font-size:16px">
      <span class="smiley"><i></i></span> {{ 'sections.header.menu' | t }}
    </div>
    <button class="x" data-close-mob aria-label="Close menu">✕</button>
  </div>
  <div class="mob-body">
    <h6>Shop</h6>
    {%- for link in linklists.main-menu.links -%}
      <a href="{{ link.url }}" data-mob>{{ link.title }} <span>→</span></a>
    {%- else -%}
      <a href="{{ routes.all_products_collection_url }}" data-mob>All Products <span>→</span></a>
    {%- endfor -%}

    <h6>Account</h6>
    {%- if customer -%}
      <a href="{{ routes.account_url }}" data-mob>Account <span>→</span></a>
      <a href="{{ routes.account_logout_url }}" data-mob>Log out <span>→</span></a>
    {%- else -%}
      <a href="{{ routes.account_login_url }}" data-mob>Log in <span>→</span></a>
      <a href="{{ routes.account_register_url }}" data-mob>Create account <span>→</span></a>
    {%- endif -%}
  </div>
  <div class="mob-foot">
    <a href="#" data-open-search>Search</a>
    <a href="#" data-open-cart>Cart</a>
  </div>
</aside>
